//Y
function mayorYMenorYPar(num) {
	if( número > 5 && número < 10 && número % 2 === 0 ) consola.log(verdadero);
	más consola.log(falso);
};
mayorYMenorYPar(7);
mayorYMenorYPar(8);

// O
función operadorOr(str) {
    if (str === 'Henry' || str.length < 2) console.log(true);
    más consola.log(falso);
}
 operadorOr('Henry');
 operadorOr('Javascript');
 operadorOr('H');

// NO
 function negacion(permiso) {
    if (permiso) console.log('Tiene permiso');
 }
 negación(verdadero);
 negacion(falso);
